language: python
python:
  - "3.9"
# command to install dependencies
install:
  - "pip install -r requirements.txt"
# command to run tests
script: python manage.py test

jobs:
  - DATABASE_USER=fikanote_travis
  - DATABASE_PASSWORD=tNcZyFGz6VW
